<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.spring.house.faq.dao.FaqMapper">
  <select id="getFaqList" resultType="kr.spring.house.faq.vo.FaqVO">
    SELECT 
      FAQ_ID AS faqId,
      CATEGORY,
      QUESTION,
      TO_CHAR(ANSWER) AS answer,
      ORDER_NUM,
      IS_ACTIVE,
      CREATED_AT,
      UPDATED_AT
    FROM FTEAM02.FAQ
    WHERE IS_ACTIVE = 'Y'
    ORDER BY ORDER_NUM ASC
  </select>
</mapper>
